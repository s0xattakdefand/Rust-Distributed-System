[package]
name    = "bff"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
actix-rt  = "2"        # ← add runtime so the test macro resolves
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
