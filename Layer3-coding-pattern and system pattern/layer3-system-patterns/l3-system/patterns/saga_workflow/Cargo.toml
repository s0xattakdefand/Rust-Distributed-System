[package]
name = "saga_workflow"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow       = "1"
layer2_saga  = { path = "../layer2_saga" }   # ← fixed
tokio        = { version = "1", features = ["rt", "macros"] }


